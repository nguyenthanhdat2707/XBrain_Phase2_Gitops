labels:
  - pairs:
      environment: preview
      preview.mini-book-hub.io/profile: book-service
    includeSelectors: false

resources:
  - ../../book-service/base

patches:
  - target:
      kind: Rollout
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
