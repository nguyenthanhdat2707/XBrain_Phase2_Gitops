labels:
  - pairs:
      environment: preview
      preview.mini-book-hub.io/profile: frontend
    includeSelectors: false

resources:
  - ../../frontend/base

patches:
  - target:
      kind: Rollout
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
